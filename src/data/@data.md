# data/

Acceso a datos del navegador. JsonLens no tiene backend todavía.

| Carpeta | Uso |
| ------- | --- |
| `browser/` | localStorage, FileReader, clipboard |

Si en el futuro hay API HTTP, crear `data/api/` sin mezclarla con helpers de `lib/`.
