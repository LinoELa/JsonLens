# Shared

Piezas reutilizables entre modulos: storage del navegador, componentes UI comunes, utils y errores de dominio.

Incluye: `domain`, `infrastructure` (con `browser-storage/`, `ui/`, `utils/`).
